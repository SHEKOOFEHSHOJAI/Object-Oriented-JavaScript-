import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals

// jsx alow create ghaleb sabk html
//bable code haye html convert mikoneh  be jsx

//tafovot html with jsx 

//class kalmeh kelidi rezerv shode hast  dar javascript

//ReactDom =>render componnet to dom 

//react har no data ra be string convert mikonad
//boolean object array  in react can't output
//